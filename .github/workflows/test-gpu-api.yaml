name: Tests (GPU API)

on:
  push:
    branches: [main, dev]
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pytest:
    uses: finki-hub/.github/.github/workflows/pytest.yaml@main
    with:
      working-directory: ./gpu-api
      sync-args: --frozen
      pytest-args: -q
